class Erc20Token {
    constructor(address, name, symbol, totalSupply) {
        this.address = address;
        this.name = name;
        this.symbol = symbol;
        this.totalSupply = totalSupply;
    }
}

export default Erc20Token;